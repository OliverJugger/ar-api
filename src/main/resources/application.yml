spring:
  application:
    name: ar-wc3-backend
  datasource:
    url: ${ORACLE_URL:jdbc:oracle:thin:@//SRVVBDDARTH01:1551/GEREP19C}
    username: ${ORACLE_USERNAME:arthus}
    password: ${ORACLE_PASSWORD:iFUo9z6MpKRt7YCk_4No}
    driver-class-name: oracle.jdbc.OracleDriver
  flyway:
    baseline-on-migrate: true
    baseline-version: 1
  cache:
    type: none

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8080

logging:
  level:
    com.arwc3.backend: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
